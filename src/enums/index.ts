export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc',
}

export enum SortDirectionCycle {
  LOWEST_FIRST = 1,
  HIGHEST_FIRST = 2,
}

export enum KeyCode {
  ESC = 27,
  ARROW_UP = 38,
  ARROW_DOWN = 40,
}
