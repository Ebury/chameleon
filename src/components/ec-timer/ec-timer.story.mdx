import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import EcTimer from './ec-timer';

<Meta title="Timer" component={EcTimer} />

# EcTimer

This is `EcTimer` component it's a component visualize a countdown in seconds that has a visual indicator 

<Story name="basic">
  <EcTimer />
</Story>

<ArgsTable story="basic" />

This is `EcTimer` component stopped:

<Canvas>
  <Story name="stopped">
    <EcTimer />
  </Story>
</Canvas>
