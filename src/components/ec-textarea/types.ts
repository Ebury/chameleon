export interface TextareaProps {
  modelValue?: string,
  rows?: number,
  label?: string,
  labelTooltip?: string,
  note?: string,
  bottomNote?: string,
  errorMessage?: string,
  id?: string,
  errorId?: string,
  isSensitive?: boolean,
  isWarning?: boolean,
  autocomplete?: string,
}
