/* import { mount } from '@vue/test-utils';
import { defineComponent } from 'vue';

import EcSummaryInfo from './ec-summary-info.vue'; */

/* describe('EcSummaryInfo', () => {
  function mountSummaryInfo(props, mountOpts) {
    return mount(EcSummaryInfo, {
      props: {
        ...props,
      },
      ...mountOpts,
    });
  }

  function mountSummaryInfoAsTemplate(template, props, wrapperComponentOpts, mountOpts) {
    const Component = defineComponent({
      components: { EcSummaryInfo },
      template,
      ...wrapperComponentOpts,
    });

    return mount(Component, {
      props,
      ...mountOpts,
    });
  }
}); */
